# 在线加密文件解密SAAS平台 - 前端设计文档

## 1. 文档说明

### 1.1 文档目的
本文档描述前端架构设计、页面结构、组件设计、状态管理等，为前端开发提供指导。

### 1.2 技术选型

| 技术 | 版本 | 说明 |
|------|------|------|
| Nuxt.js | 4.x | Vue.js SSR框架 |
| Vue | 3.4+ | 渐进式JavaScript框架 |
| TypeScript | 5.x | 类型安全 |
| Tailwind CSS | 3.x | 原子化CSS框架 |
| shadcn-vue | latest | 高质量组件库 |
| Pinia | 2.x | 状态管理 |
| VueUse | 10.x | Composition API工具集 |
| VeeValidate | 4.x | 表单验证 |
| Axios | 1.x | HTTP客户端 |
| Socket.io-client | 2.x | WebSocket客户端 |
| Vue I18n | 9.x | 国际化 |

### 1.3 浏览器支持
- Chrome (最新两个版本)
- Firefox (最新两个版本)
- Safari (最新两个版本)
- Edge (最新两个版本)

## 2. 项目结构

```
frontend/
├── .nuxt/                    # Nuxt自动生成
├── node_modules/             # 依赖包
├── public/                   # 静态资源
│   ├── favicon.ico
│   └── images/
├── server/                   # 服务端中间件
│   └── api/
├── types/                    # 全局类型定义
│   ├── global.d.ts
│   └── shims.vue.d.ts
├── .env                      # 环境变量
├── .env.development          # 开发环境变量
├── .env.production           # 生产环境变量
├── app.vue                   # 根组件
├── nuxt.config.ts            # Nuxt配置
├── tailwind.config.js        # Tailwind配置
├── tsconfig.json             # TypeScript配置
├── assets/                   # 资源文件
│   ├── css/
│   │   └── main.css
│   └── images/
├── components/               # 组件
│   ├── common/               # 通用组件
│   ├── features/             # 功能组件
│   └── layouts/              # 布局组件
├── composables/              # 组合式函数
├── layouts/                  # 页面布局
│   ├── default.vue           # 默认布局
│   ├── auth.vue              # 认证布局
│   └── empty.vue             # 空白布局
├── middleware/               # 中间件
│   ├── auth.ts               # 认证中间件
│   ├── guest.ts              # 访客中间件
│   └── logger.ts             # 日志中间件
├── pages/                    # 页面
│   ├── index.vue             # 首页
│   ├── login.vue             # 登录
│   ├── register.vue          # 注册
│   ├── 404.vue               # 404页面
│   ├── 500.vue               # 500页面
│   ├── tasks/                # 任务相关
│   ├── files/                # 文件相关
│   ├── profile/              # 个人中心
│   └── pricing.vue           # 价格页
├── plugins/                  # 插件
│   ├── api.ts                # API插件
│   ├── i18n.ts               # 国际化插件
│   ├── dayjs.ts              # 日期插件
│   └── toast.ts              # 提示插件
├── stores/                   # Pinia状态
│   ├── user.ts               # 用户状态
│   ├── task.ts               # 任务状态
│   ├── file.ts               # 文件状态
│   ├── ui.ts                 # UI状态
│   └── notification.ts       # 通知状态
├── utils/                    # 工具函数
│   ├── request.ts            # HTTP请求封装
│   ├── validate.ts           # 验证函数
│   ├── format.ts             # 格式化函数
│   ├── storage.ts            # 本地存储
│   └── constants.ts          # 常量
└── locales/                  # 国际化文件
    ├── zh-CN.json            # 简体中文
    └── en-US.json            # 英文
```

## 3. 页面设计

### 3.1 页面路由

| 路径 | 页面 | 布局 | 中间件 | 说明 |
|------|------|------|--------|------|
| / | 首页 | default | - | 产品介绍 |
| /login | 登录 | auth | guest | 用户登录 |
| /register | 注册 | auth | guest | 用户注册 |
| /forgot-password | 忘记密码 | auth | guest | 密码重置 |
| /reset-password | 重置密码 | auth | guest | 设置新密码 |
| /pricing | 价格 | default | - | 定价方案 |
| /upload | 上传文件 | default | auth | 文件上传 |
| /tasks | 任务列表 | default | auth | 我的任务 |
| /tasks/create | 创建任务 | default | auth | 创建新任务 |
| /tasks/[id] | 任务详情 | default | auth | 任务详细信息 |
| /files | 文件管理 | default | auth | 文件列表 |
| /profile | 个人中心 | default | auth | 个人信息 |
| /profile/security | 安全设置 | default | auth | 修改密码 |
| /profile/billing | 账单管理 | default | auth | 订单记录 |
| /profile/api | API管理 | default | auth | 企业API |
| /settings | 设置 | default | auth | 系统设置 |
| /docs/faq | 帮助文档 | default | - | FAQ |
| /api/* | API代理 | - | - | 后端API |

### 3.2 页面布局设计

#### 3.2.1 默认布局 (layouts/default.vue)

```
┌─────────────────────────────────────────────────────────────┐
│  Header (顶部导航)                                            │
│  Logo | 首页 | 服务 | 价格 | 文档 | 登录/用户头像             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Main Content (页面内容)                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │  <slot />                                          │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  Footer (底部)                                               │
│  关于 | 服务条款 | 隐私政策 | 联系我们 | 社交媒体             │
│  © 2024 Company Name. All rights reserved.                  │
└─────────────────────────────────────────────────────────────┘
```

#### 3.2.2 认证布局 (layouts/auth.vue)

```
┌─────────────────────────────────────────────────────────────┐
│  Logo                                                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────┐                    │
│  │  左侧：品牌宣传图                     │  右侧：           │
│  │  产品介绍                             │  ┌─────────────┐ │
│  │  核心功能                             │  │             │ │
│  │  用户评价                             │  │  <slot />   │ │
│  │                                     │  │             │ │
│  │                                     │  │             │ │
│  └─────────────────────────────────────┘  └─────────────┘ │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 核心页面设计

#### 3.3.1 首页 (pages/index.vue)

**区块划分**:
1. **Hero Section** - 主视觉区
   - 标题："忘记密码？我们帮您找回"
   - 副标题："快速、安全、可靠"
   - CTA按钮："立即开始"
   - 产品预览图

2. **Features Section** - 功能特性
   - 卡片式布局展示核心功能
   - 图标 + 标题 + 描述

3. **How It Works** - 使用流程
   - 步骤1：上传文件
   - 步骤2：选择方案
   - 步骤3：接收结果

4. **Pricing Section** - 价格方案
   - 三列布局：免费/高级/企业

5. **Testimonials** - 用户评价
   - 轮播展示用户评价

6. **CTA Section** - 行动召唤
   - "立即开始使用"

#### 3.3.2 任务列表页 (pages/tasks/index.vue)

**布局**:
```
┌─────────────────────────────────────────────────────────────┐
│  面包屑: 首页 > 任务管理                                       │
├─────────────────────────────────────────────────────────────┤
│  筛选栏: [全部] [处理中] [已完成] [失败]                        │
│  搜索框: [搜索任务...]                                        │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 任务卡片1                                            │   │
│  │ 任务名称 | 文件名 | 状态 | 进度条 | 创建时间            │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 任务卡片2                                            │   │
│  └─────────────────────────────────────────────────────┘   │
│  ...                                                        │
├─────────────────────────────────────────────────────────────┤
│  分页: [上一页] 1 2 3 4 5 [下一页]                           │
└─────────────────────────────────────────────────────────────┘
```

#### 3.3.3 创建任务页 (pages/tasks/create.vue)

**步骤流程**:
```
步骤1: 上传文件 → 步骤2: 选择方案 → 步骤3: 确认提交 → 步骤4: 完成
```

**表单内容**:
1. 文件上传（拖拽区域）
2. 任务信息
   - 任务名称
   - 密码提示（可选）
3. 破解方案选择
   - 单选卡片布局
   - 显示价格和预计时间
4. 通知设置
   - 邮件通知
   - 短信通知
5. 提交按钮

#### 3.3.4 任务详情页 (pages/tasks/[id].vue)

**内容区块**:
1. **任务基本信息**
   - 任务名称
   - 创建时间
   - 任务状态

2. **文件信息**
   - 文件名
   - 文件大小
   - 文件类型

3. **破解配置**
   - 破解方案
   - 密码提示

4. **进度显示**
   - 进度条
   - 当前尝试的密码（可选显示）
   - 预计剩余时间

5. **破解结果**（完成后显示）
   - 密码（遮罩显示，点击查看）
   - 尝试次数
   - 耗时

6. **操作按钮**
   - 取消任务
   - 重新尝试
   - 下载结果

#### 3.3.5 文件上传页 (pages/upload.vue)

**上传组件**:
```
┌─────────────────────────────────────────────┐
│                                             │
│          📁                                 │
│   拖拽文件到这里，或点击选择文件                │
│                                             │
│   支持格式：ZIP, RAR, 7Z, PDF等              │
│   最大文件：50MB (免费) / 2GB (企业)         │
│                                             │
└─────────────────────────────────────────────┘
```

**上传列表**:
- 文件名
- 大小
- 上传进度条
- 状态（上传中/完成/失败）
- 删除按钮

#### 3.3.6 个人中心页 (pages/profile/index.vue)

**Tab页签**:
1. **基本信息**
   - 头像上传
   - 用户名
   - 邮箱
   - 手机号

2. **会员信息**
   - 当前会员等级
   - 到期时间
   - 会员权益
   - 续费/升级按钮

3. **统计数据**
   - 总任务数
   - 成功任务数
   - 总消费

4. **最近活动**
   - 最近任务列表
   - 最近订单列表

## 4. 组件设计

### 4.1 通用组件 (components/common/)

#### 4.1.1 AppHeader.vue - 头部导航

```vue
<template>
  <header class="app-header">
    <div class="header-left">
      <NuxtLink to="/" class="logo">
        <img src="~/assets/images/logo.svg" alt="Logo" />
        <span>FilePassword</span>
      </NuxtLink>
      <nav class="main-nav">
        <NuxtLink to="/services">{{ $t('nav.services') }}</NuxtLink>
        <NuxtLink to="/pricing">{{ $t('nav.pricing') }}</NuxtLink>
        <NuxtLink to="/docs">{{ $t('nav.docs') }}</NuxtLink>
      </nav>
    </div>
    <div class="header-right">
      <div v-if="isAuthenticated" class="user-menu">
        <NuxtLink to="/tasks" class="nav-link">{{ $t('nav.tasks') }}</NuxtLink>
        <VDropdown>
          <button class="user-avatar">
            <img :src="user.avatar" :alt="user.username" />
          </button>
          <template #popper>
            <div class="dropdown-menu">
              <div class="user-info">
                <div class="username">{{ user.username }}</div>
                <div class="membership">{{ $t(`membership.${user.membership}`) }}</div>
              </div>
              <div class="menu-items">
                <NuxtLink to="/profile">{{ $t('nav.profile') }}</NuxtLink>
                <NuxtLink to="/tasks">{{ $t('nav.tasks') }}</NuxtLink>
                <NuxtLink to="/files">{{ $t('nav.files') }}</NuxtLink>
                <NuxtLink to="/settings">{{ $t('nav.settings') }}</NuxtLink>
                <div class="divider"></div>
                <button @click="logout">{{ $t('nav.logout') }}</button>
              </div>
            </div>
          </template>
        </VDropdown>
      </div>
      <div v-else class="auth-buttons">
        <Button variant="ghost" to="/login">{{ $t('auth.login') }}</Button>
        <Button variant="primary" to="/register">{{ $t('auth.register') }}</Button>
      </div>
      <LanguageSwitcher />
    </div>
  </header>
</template>
```

**Props**: 无

**Emits**: 无

**依赖**:
- `useUserStore` - 用户状态
- `useAuth` - 认证逻辑

#### 4.1.2 AppFooter.vue - 底部

```vue
<template>
  <footer class="app-footer">
    <div class="footer-content">
      <div class="footer-section">
        <h3>产品</h3>
        <ul>
          <li><NuxtLink to="/services">服务介绍</NuxtLink></li>
          <li><NuxtLink to="/pricing">价格方案</NuxtLink></li>
          <li><NuxtLink to="/enterprise">企业版</NuxtLink></li>
        </ul>
      </div>
      <div class="footer-section">
        <h3>资源</h3>
        <ul>
          <li><NuxtLink to="/docs/faq">常见问题</NuxtLink></li>
          <li><NuxtLink to="/docs/tutorial">使用教程</NuxtLink></li>
          <li><NuxtLink to="/docs/api">API文档</NuxtLink></li>
        </ul>
      </div>
      <div class="footer-section">
        <h3>公司</h3>
        <ul>
          <li><NuxtLink to="/about">关于我们</NuxtLink></li>
          <li><NuxtLink to="/contact">联系我们</NuxtLink></li>
        </ul>
      </div>
      <div class="footer-section">
        <h3>关注我们</h3>
        <div class="social-links">
          <a href="#" class="social-link"><IconWechat /></a>
          <a href="#" class="social-link"><IconWeibo /></a>
          <a href="#" class="social-link"><IconGithub /></a>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; 2024 FilePassword. All rights reserved.</p>
      <div class="legal-links">
        <NuxtLink to="/terms">服务条款</NuxtLink>
        <span class="separator">|</span>
        <NuxtLink to="/privacy">隐私政策</NuxtLink>
      </div>
    </div>
  </footer>
</template>
```

#### 4.1.3 LoadingSpinner.vue - 加载组件

```vue
<template>
  <div class="loading-spinner" :class="[`size-${size}`, variant]">
    <div class="spinner"></div>
    <p v-if="text" class="loading-text">{{ text }}</p>
  </div>
</template>

<script setup lang="ts">
interface Props {
  size?: 'sm' | 'md' | 'lg'
  variant?: 'default' | 'primary' | 'white'
  text?: string
}

withDefaults(defineProps<Props>(), {
  size: 'md',
  variant: 'default'
})
</script>
```

#### 4.1.4 EmptyState.vue - 空状态

```vue
<template>
  <div class="empty-state">
    <div class="empty-icon">
      <component :is="icon" />
    </div>
    <h3 class="empty-title">{{ title }}</h3>
    <p class="empty-description">{{ description }}</p>
    <slot name="actions">
      <Button v-if="actionText" @click="$emit('action')">
        {{ actionText }}
      </Button>
    </slot>
  </div>
</template>

<script setup lang="ts">
interface Props {
  icon?: any
  title: string
  description: string
  actionText?: string
}

defineProps<Props>()
defineEmits<{
  action: []
}>()
</script>
```

### 4.2 功能组件 (components/features/)

#### 4.2.1 FileUploader.vue - 文件上传

```vue
<template>
  <div class="file-uploader">
    <div
      class="upload-area"
      :class="{ 'is-dragging': isDragging, 'is-disabled': disabled }"
      @dragover.prevent="handleDragOver"
      @dragleave.prevent="handleDragLeave"
      @drop.prevent="handleDrop"
      @click="handleClick"
    >
      <input
        ref="fileInput"
        type="file"
        :accept="accept"
        :multiple="multiple"
        @change="handleFileChange"
      />
      <div class="upload-content">
        <IconUpload class="upload-icon" />
        <p class="upload-hint">{{ $t('upload.drag_hint') }}</p>
        <p class="upload-sub">{{ $t('upload.or_click') }}</p>
        <p class="upload-limit">
          {{ $t('upload.supported_formats') }}: {{ formats.join(', ') }}
          <br />
          {{ $t('upload.max_size') }}: {{ formatFileSize(maxSize) }}
        </p>
      </div>
    </div>
    <div v-if="files.length > 0" class="file-list">
      <FileItem
        v-for="file in files"
        :key="file.id"
        :file="file"
        @remove="handleRemove"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  accept?: string
  maxSize?: number
  formats?: string[]
  multiple?: boolean
  disabled?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  maxSize: 50 * 1024 * 1024, // 50MB
  formats: () => ['zip', 'rar', '7z', 'pdf', 'doc', 'docx'],
  multiple: false
})

interface FileWithId extends File {
  id: string
  progress: number
  status: 'pending' | 'uploading' | 'success' | 'error'
}

const files = ref<FileWithId[]>([])
const isDragging = ref(false)
const fileInput = ref<HTMLInputElement>()

// 上传逻辑...
</script>
```

#### 4.2.2 TaskCard.vue - 任务卡片

```vue
<template>
  <div class="task-card" @click="goToDetail">
    <div class="task-header">
      <div class="task-name">{{ task.task_name }}</div>
      <TaskStatusBadge :status="task.status" />
    </div>
    <div class="task-body">
      <div class="task-file">
        <IconFile />
        <span>{{ task.file_name }}</span>
        <span class="file-size">{{ formatFileSize(task.file_size) }}</span>
      </div>
      <div class="task-config">
        <Tag>{{ $t(`crack_type.${task.crack_type}`) }}</Tag>
      </div>
    </div>
    <div v-if="task.status === 'processing'" class="task-progress">
      <ProgressBar :percentage="task.progress" />
      <div class="progress-info">
        <span>{{ task.progress }}%</span>
        <span v-if="task.estimated_time">
          {{ $t('task.estimated_time') }}: {{ formatTime(task.estimated_time) }}
        </span>
      </div>
    </div>
    <div class="task-footer">
      <span class="create-time">{{ formatDateTime(task.created_at) }}</span>
      <div class="task-actions">
        <Button v-if="canCancel" size="sm" variant="ghost" @click.stop="handleCancel">
          {{ $t('task.cancel') }}
        </Button>
        <Button v-if="canRetry" size="sm" variant="ghost" @click.stop="handleRetry">
          {{ $t('task.retry') }}
        </Button>
        <Button size="sm" @click.stop="goToDetail">
          {{ $t('common.detail') }}
        </Button>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  task: Task
}

const props = defineProps<Props>()

const canCancel = computed(() =>
  ['pending', 'queued', 'analyzing'].includes(props.task.status)
)
const canRetry = computed(() => props.task.status === 'failed')

function goToDetail() {
  navigateTo(`/tasks/${props.task.task_id}`)
}

async function handleCancel() {
  // 取消任务逻辑
}

async function handleRetry() {
  // 重试任务逻辑
}
</script>
```

#### 4.2.3 CrackTypeSelector.vue - 破解方案选择器

```vue
<template>
  <div class="crack-type-selector">
    <div
      v-for="option in options"
      :key="option.type"
      class="crack-type-card"
      :class="{ 'is-selected': modelValue === option.type }"
      @click="select(option.type)"
    >
      <div class="card-header">
        <div class="card-icon">
          <component :is="option.icon" />
        </div>
        <div class="card-title">{{ option.name }}</div>
        <div v-if="option.tag" class="card-tag">{{ option.tag }}</div>
      </div>
      <div class="card-body">
        <p class="card-description">{{ option.description }}</p>
        <ul class="card-features">
          <li v-for="feature in option.features" :key="feature">
            <IconCheck /> {{ feature }}
          </li>
        </ul>
      </div>
      <div class="card-footer">
        <div class="card-price">
          <span class="price">{{ option.price > 0 ? `¥${option.price}` : $t('common.free') }}</span>
          <span v-if="option.price > 0" class="unit">/次</span>
        </div>
        <div class="card-time">
          <IconClock />
          <span>{{ option.time || $t('task.custom') }}</span>
        </div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
interface CrackTypeOption {
  type: CrackType
  name: string
  description: string
  icon: any
  tag?: string
  features: string[]
  price: number
  time?: string
}

interface Props {
  modelValue: CrackType
  options: CrackTypeOption[]
}

defineProps<Props>()
const emit = defineEmits<{
  'update:modelValue': [value: CrackType]
}>()

function select(type: CrackType) {
  emit('update:modelValue', type)
}
</script>
```

#### 4.2.4 ProgressBar.vue - 进度条

```vue
<template>
  <div class="progress-bar" :class="{ 'is-animated': animated, 'is-striped': striped }">
    <div
      class="progress-fill"
      :class="[`variant-${variant}`]"
      :style="{ width: `${percentage}%` }"
    >
      <span v-if="showText" class="progress-text">{{ percentage }}%</span>
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  percentage: number
  variant?: 'primary' | 'success' | 'warning' | 'danger'
  striped?: boolean
  animated?: boolean
  showText?: boolean
}

withDefaults(defineProps<Props>(), {
  variant: 'primary',
  striped: false,
  animated: false,
  showText: false
})
</script>
```

### 4.3 UI组件 (components/ui/)

基于 shadcn-vue 的组件封装：

- Button.vue - 按钮
- Input.vue - 输入框
- Textarea.vue - 文本域
- Select.vue - 下拉选择
- Checkbox.vue - 复选框
- Radio.vue - 单选框
- Switch.vue - 开关
- Modal.vue - 模态框
- Dropdown.vue - 下拉菜单
- Tooltip.vue - 提示框
- Toast.vue - 消息提示
- Table.vue - 表格
- Pagination.vue - 分页
- Tag.vue - 标签
- Badge.vue - 徽章
- Avatar.vue - 头像

## 5. 状态管理 (Pinia)

### 5.1 用户状态 (stores/user.ts)

```typescript
import { defineStore } from 'pinia'

export const useUserStore = defineStore('user', {
  state: () => ({
    user: null as User | null,
    token: null as string | null,
    refreshToken: null as string | null,
    isAuthenticated: false,
    membership: null as Membership | null
  }),

  getters: {
    userId: (state) => state.user?.user_id,
    username: (state) => state.user?.username,
    email: (state) => state.user?.email,
    avatar: (state) => state.user?.avatar,
    isPremium: (state) => state.user?.membership === 'premium',
    isEnterprise: (state) => state.user?.membership === 'enterprise'
  },

  actions: {
    async login(credentials: LoginCredentials) {
      const { data } = await api.post('/auth/login', credentials)
      this.setUser(data)
      return data
    },

    async register(info: RegisterInfo) {
      const { data } = await api.post('/auth/register', info)
      this.setUser(data)
      return data
    },

    async logout() {
      await api.post('/auth/logout')
      this.clearUser()
    },

    async fetchUser() {
      const { data } = await api.get('/users/me')
      this.user = data
      return data
    },

    setUser(data: AuthResponse) {
      this.user = data.user
      this.token = data.access_token
      this.refreshToken = data.refresh_token
      this.isAuthenticated = true
      // 保存到localStorage
    },

    clearUser() {
      this.user = null
      this.token = null
      this.refreshToken = null
      this.isAuthenticated = false
      // 清除localStorage
    },

    async updateProfile(info: Partial<User>) {
      const { data } = await api.patch('/users/me', info)
      this.user = { ...this.user, ...data }
      return data
    }
  }
})
```

### 5.2 任务状态 (stores/task.ts)

```typescript
import { defineStore } from 'pinia'

export const useTaskStore = defineStore('task', {
  state: () => ({
    tasks: [] as Task[],
    currentTask: null as Task | null,
    loading: false,
    total: 0,
    filters: {
      status: '',
      crackType: '',
      page: 1,
      pageSize: 20
    }
  }),

  getters: {
    pendingTasks: (state) => state.tasks.filter(t => t.status === 'pending'),
    processingTasks: (state) => state.tasks.filter(t => t.status === 'processing'),
    completedTasks: (state) => state.tasks.filter(t => t.status === 'completed'),
    failedTasks: (state) => state.tasks.filter(t => t.status === 'failed')
  },

  actions: {
    async fetchTasks(params?: TaskQueryParams) {
      this.loading = true
      try {
        const { data } = await api.get('/tasks', { params })
        this.tasks = data.items
        this.total = data.total
        return data
      } finally {
        this.loading = false
      }
    },

    async fetchTask(taskId: string) {
      const { data } = await api.get(`/tasks/${taskId}`)
      this.currentTask = data
      return data
    },

    async createTask(taskInfo: CreateTaskDto) {
      const { data } = await api.post('/tasks', taskInfo)
      this.tasks.unshift(data)
      return data
    },

    async cancelTask(taskId: string) {
      await api.post(`/tasks/${taskId}/cancel`)
      const task = this.tasks.find(t => t.task_id === taskId)
      if (task) {
        task.status = 'cancelled'
      }
    },

    async retryTask(taskId: string, config?: CrackConfig) {
      const { data } = await api.post(`/tasks/${taskId}/retry`, { crack_config: config })
      return data
    },

    updateTaskProgress(taskId: string, progress: number, status: string) {
      const task = this.tasks.find(t => t.task_id === taskId)
      if (task) {
        task.progress = progress
        task.status = status
      }
    },

    subscribeToTask(taskId: string) {
      // WebSocket订阅任务进度
    }
  }
})
```

### 5.3 UI状态 (stores/ui.ts)

```typescript
import { defineStore } from 'pinia'

export const useUiStore = defineStore('ui', {
  state: () => ({
    sidebarOpen: true,
    theme: 'light' as 'light' | 'dark',
    language: 'zh-CN',
    loading: false,
    toast: [] as Toast[],
    modal: null as ModalConfig | null
  }),

  actions: {
    toggleSidebar() {
      this.sidebarOpen = !this.sidebarOpen
    },

    setTheme(theme: 'light' | 'dark') {
      this.theme = theme
      // 保存到localStorage
    },

    setLanguage(lang: string) {
      this.language = lang
      // 切换i18n语言
    },

    showToast(toast: Omit<Toast, 'id'>) {
      const id = Date.now().toString()
      this.toast.push({ id, ...toast })
      setTimeout(() => this.removeToast(id), toast.duration || 3000)
    },

    removeToast(id: string) {
      const index = this.toast.findIndex(t => t.id === id)
      if (index > -1) {
        this.toast.splice(index, 1)
      }
    },

    openModal(modal: ModalConfig) {
      this.modal = modal
    },

    closeModal() {
      this.modal = null
    }
  }
})
```

## 6. 组合式函数 (Composables)

### 6.1 useAuth.ts - 认证相关

```typescript
export function useAuth() {
  const userStore = useUserStore()
  const router = useRouter()

  const login = async (credentials: LoginCredentials) => {
    try {
      await userStore.login(credentials)
      router.push('/')
    } catch (error) {
      throw error
    }
  }

  const register = async (info: RegisterInfo) => {
    try {
      await userStore.register(info)
      router.push('/')
    } catch (error) {
      throw error
    }
  }

  const logout = async () => {
    await userStore.logout()
    router.push('/login')
  }

  return {
    user: computed(() => userStore.user),
    isAuthenticated: computed(() => userStore.isAuthenticated),
    login,
    register,
    logout
  }
}
```

### 6.2 useApi.ts - API调用

```typescript
export function useApi() {
  const config = useRuntimeConfig()
  const userStore = useUserStore()

  const api = $fetch.create({
    baseURL: config.public.apiBase,
    onRequest({ options }) {
      // 添加认证token
      if (userStore.token) {
        options.headers = {
          ...options.headers,
          Authorization: `Bearer ${userStore.token}`
        }
      }
    },
    async onResponseError({ response }) {
      if (response.status === 401) {
        // Token过期，尝试刷新
        await userStore.refreshToken()
      }
    }
  })

  return { api }
}
```

### 6.3 useUpload.ts - 文件上传

```typescript
export function useUpload() {
  const uploading = ref(false)
  const progress = ref(0)
  const { api } = useApi()

  const uploadFile = async (file: File, onProgress?: (p: number) => void) => {
    uploading.value = true
    progress.value = 0

    try {
      // 获取预签名URL
      const { data: uploadInfo } = await api.post('/files/upload-url', {
        file_name: file.name,
        file_size: file.size,
        content_type: file.type
      })

      // 上传到对象存储
      await axios.put(uploadInfo.upload_url, file, {
        onUploadProgress: (e) => {
          progress.value = Math.round((e.loaded * 100) / e.total!)
          onProgress?.(progress.value)
        }
      })

      // 确认上传完成
      const { data } = await api.post(`/files/${uploadInfo.file_id}/confirm`, {
        etag: '',
        file_size: file.size
      })

      return data
    } finally {
      uploading.value = false
    }
  }

  return {
    uploading,
    progress,
    uploadFile
  }
}
```

### 6.4 useWebSocket.ts - WebSocket连接

```typescript
export function useWebSocket(taskId: string) {
  const connected = ref(false)
  const data = ref<any>(null)
  let ws: WebSocket | null = null

  const connect = () => {
    const config = useRuntimeConfig()
    const userStore = useUserStore()
    const url = `${config.public.wsBase}/ws/tasks/${taskId}?token=${userStore.token}`

    ws = new WebSocket(url)

    ws.onopen = () => {
      connected.value = true
    }

    ws.onmessage = (event) => {
      const message = JSON.parse(event.data)
      data.value = message
    }

    ws.onerror = (error) => {
      console.error('WebSocket error:', error)
    }

    ws.onclose = () => {
      connected.value = false
    }
  }

  const disconnect = () => {
    ws?.close()
    ws = null
  }

  onUnmounted(() => {
    disconnect()
  })

  return {
    connected,
    data,
    connect,
    disconnect
  }
}
```

## 7. 样式规范

### 7.1 设计令牌

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9',
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e'
        }
      },
      spacing: {
        '18': '4.5rem',
        '88': '22rem',
        '128': '32rem'
      },
      borderRadius: {
        '4xl': '2rem'
      }
    }
  }
}
```

### 7.2 组件样式约定

- 使用BEM命名规范
- 响应式优先（mobile-first）
- 支持深色模式
- 支持RTL布局

## 8. 性能优化

### 8.1 代码分割

- 路由级别代码分割
- 组件懒加载
- 动态导入

### 8.2 资源优化

- 图片压缩和WebP格式
- 字体子集化
- CSS/JS压缩

### 8.3 缓存策略

- Service Worker缓存
- HTTP缓存头
- LocalStorage缓存

## 9. 国际化

### 9.1 支持语言

- 简体中文 (zh-CN) - 默认
- English (en-US)

### 9.2 翻译文件结构

```json
// locales/zh-CN.json
{
  "nav": {
    "services": "服务",
    "pricing": "价格",
    "tasks": "任务"
  },
  "auth": {
    "login": "登录",
    "register": "注册"
  },
  "task": {
    "status": {
      "pending": "等待中",
      "processing": "处理中",
      "completed": "已完成",
      "failed": "失败"
    }
  }
}
```

---

**文档版本：1.0**
**创建日期：2024-12-24**
**文档作者：前端架构师**
